import assert from 'node:assert/strict';import test from 'node:test';import worker from '../worker-entry.js';const ev=()=>new Request('https://homequotecheck.co.uk/api/analyse',{method:'POST',headers:{'content-type':'application/json','x-hqc-technology':'ev_chargepoint'},body:JSON.stringify({quoteText:'Installation of Zappi V2 EV charger 7.4 kW charging. Total £1,250.'})});test('EV stays closed without internal gate',async()=>{const r=await worker.fetch(ev(),{},{});assert.equal(r.status,409);assert.equal((await r.json()).technology,'ev_chargepoint')});test('EV internal gate invokes only EV adapter',async()=>{const r=await worker.fetch(ev(),{HQC_EV_CHARGEPOINT_ANALYSIS_INTERNAL:'1'},{});assert.equal(r.status,200);assert.equal(r.headers.get('x-hqc-analysis-technology'),'ev_chargepoint');assert.equal(r.headers.get('x-hqc-analysis-adapter'),'ev_chargepoint_internal');const j=await r.json();assert.equal(j.technology,'ev_chargepoint');assert.equal(j.evidence.powerKw,7.4)});test('Solar flag cannot open EV',async()=>{const r=await worker.fetch(ev(),{HQC_SOLAR_ANALYSIS_PUBLIC:'1'},{});assert.equal(r.status,409)});