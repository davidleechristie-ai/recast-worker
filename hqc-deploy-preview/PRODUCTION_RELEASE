2026-09-11T17:28+01:00
Emergency production repair: restore missing primary stylesheet on homequotecheck.co.uk.

Incident observed on production:
- index.html references /assets/index-CjgMTR3j.css;
- Cloudflare asset bundle does not contain that stylesheet;
- SPA fallback therefore returns index.html with content-type text/html for the CSS URL;
- the browser renders the site largely unstyled.

Action for this release:
- rebuild a fresh snapshot from the healthy upstream HQC app;
- include all current transitive CSS/JS assets;
- deploy to the existing hqc-production Cloudflare Worker;
- verify the production homepage and runtime assets after cutover.

Follow-up release gates will explicitly validate every stylesheet referenced by index.html before and after deployment.
