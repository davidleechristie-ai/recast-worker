2026-09-11T17:49+01:00
Promote canary-verified HQC website repair and growth telemetry correction to production.

Website repair verified:
- production stylesheet returns 200 text/css and snapshot guards prevent missing/HTML-fallback CSS from deploying;
- PDF upload compatibility is restored for Chromium browsers via Map upsert compatibility helpers loaded before PDF.js rendering;
- production deploy #74 succeeded;
- post-deploy site consistency run #45 passed all 18 HTML pages on mobile and desktop, required runtime MIME checks, responsive layout checks and live PDF-to-WebP conversion.

Growth telemetry correction:
- diagnostic confirmed supported landing_view events return 200 and are recorded;
- landing_variant_exposed is not supported by the existing metrics event vocabulary and returned 400;
- remove the unsupported exposure event while preserving variant attribution on the supported checker_cta_clicked conversion event;
- canary #169 passed snapshot, custom-domain verification and mobile quote-intake smoke tests.

Deploy via the existing hqc-production guarded workflow and preserve the post-deploy site-consistency audit.
