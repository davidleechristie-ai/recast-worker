2026-09-11T20:39+01:00
Promote canary-verified same-epoch extended HQC funnel measurement to production.

Measurement integrity correction:
- preserve cumulative authoritative North Star counters and their original measurement epoch;
- keep new downstream funnel counters for multi-quote analysis, Decision Case, installer questions and recipient checker starts;
- add an extendedGenuine denominator that begins with the extended funnel measurement epoch so multi-quote and Decision Case rates never mix historical and newly introduced counters;
- use sample thresholds before downstream recommendations, avoiding false bottleneck diagnoses from tiny cohorts;
- QA/demo/test traffic remains excluded, no personal data is added, and no historical values are invented or backfilled.

Canary verification:
- custom-domain canary #184 passed snapshot, Cloudflare deployment, domain/API verification and both mobile quote-intake paths;
- no AppDeploy deployment is involved;
- preserve current custom-domain routing and same-origin /api proxy.

Deploy via the existing guarded hqc-production workflow and remeasure durable production metrics before making the next growth change.
