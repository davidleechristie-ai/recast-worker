2026-09-11T19:36+01:00
Promote canary-verified HQC CTA measurement integrity fix to production.

Measurement integrity repair:
- durable production metrics are the authoritative North Star source;
- remove duplicate checker_cta_clicked emission from the landing-trust enhancement because the upstream application already emits the canonical CTA event with the `src` acquisition source;
- this prevents future double-counting and conflicting source attribution while leaving the reassurance copy unchanged;
- the scheduled commercial metrics workflow now reads only durable same-origin production metrics and refuses non-durable data;
- QA/demo/test traffic remains excluded and no historical events are invented or backfilled.

Canary verification:
- custom-domain canary #180 passed snapshot, Cloudflare deployment, domain/API verification and both mobile quote-intake paths;
- no AppDeploy deployment is involved;
- preserve current custom-domain routing and same-origin /api proxy.

Deploy via the existing guarded hqc-production workflow and verify production before using subsequent CTA ratios for growth decisions.
