2026-09-14T18:46+01:00
Promote canary-verified checker-start measurement integrity fix.

Measured state before change:
- Stripe live account: no PaymentIntents; authoritative genuine revenue remains £0;
- durable production funnel: 16 landings / 0 checker starts / 1 upload / 1 genuine analysis / 0 checkouts;
- QA/demo/headless/smoke activity excluded by durable metrics;
- SEO evidence remains weak: no reliable HQC visibility returned for targeted site/search checks, while established competitors rank for heat-pump quote checklist and comparison intent.

Diagnosis:
- the funnel contained a measurement inconsistency: a genuine upload/analysis existed while checker-start remained zero;
- landing-conversion.js assumed the upstream hero CTA always emitted checker_cta_clicked, so an upstream tracking miss could under-count the earliest funnel stage;
- making another CRO change before fixing this would risk optimising against bad data.

Hypothesis:
- a fallback checker-start beacon, emitted only when the app does not emit its own checker_cta_clicked event, will make the landing -> checker-start metric reliable without double-counting normal upstream events.

Change:
- observe outgoing /api/event calls for the existing checker_cta_clicked event;
- attach fallback tracking to hero and header start controls;
- wait 650ms after a click and emit only if no upstream checker-start event was observed;
- no new personal data, no payment changes, no SEO page, no analysis/certification/eligibility changes, no DNS changes.

Verification:
- custom-domain canary #202 completed successfully;
- canary domain verification and mobile quote-intake paths passed;
- no AppDeploy deployment involved.

Deploy only through guarded hqc-production Cloudflare workflow. Preserve homequotecheck.co.uk and www.homequotecheck.co.uk routing.
