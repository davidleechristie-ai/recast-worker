2026-09-11T18:36+01:00
Promote canary-verified HQC growth-telemetry integrity repair to production.

Measurement repair:
- production growth metrics currently operate over a 500-event backend window and were being polluted by automated headless browser audits;
- Cloudflare now forces HeadlessChrome/Playwright event traffic to isTest=true, in addition to explicit qa/release/smoke markers;
- normal production browser traffic is not classified as QA merely because it is a site-owner visit;
- genuine-analysis telemetry now has an idempotent reconciliation layer that re-emits analysis_qualified_real_quote from the saved real Decision Case using the same analysisId, so duplicate delivery cannot inflate the Set-based genuine metric but missed delivery can be repaired on results/page restore;
- demo cases and non-real manual cases remain excluded.

Verification:
- custom-domain canary #174 completed successfully through snapshot, Cloudflare deployment, domain/API verification and mobile quote-intake smoke tests;
- no AppDeploy deployment is involved;
- preserve current custom-domain routing and same-origin /api proxy.

Deploy via the existing guarded hqc-production workflow, then run the normal post-deploy site consistency audit.
