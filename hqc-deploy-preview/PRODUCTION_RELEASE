2026-09-11T17:44+01:00
Promote canary-verified stylesheet hardening and PDF upload compatibility fix to HQC production.

Website repair status:
- production stylesheet is healthy and returns 200 text/css;
- snapshot discovery accepts configured target-origin asset URLs and refuses to deploy missing, empty or HTML-fallback stylesheets;
- canary #167 passed snapshot, stylesheet validation, Cloudflare binding, domain verification and mobile quote intake.

PDF upload root cause:
- pdfjs-dist 5.4 uses Map.getOrInsertComputed / Map.getOrInsert;
- Chromium 140 used by the production regression test does not expose those helpers;
- PDF loading succeeded but page rendering failed before conversion, leaving users on the upload step.

PDF fix:
- load a small compatibility shim before the HQC upload flow;
- preserve local PDF.js and worker delivery with JavaScript MIME types;
- dedicated canary PDF test converted a generated installer quote from application/pdf to image/webp and reported PDF ready in about two seconds;
- no page errors were recorded during the successful conversion.

Production release must preserve the existing hqc-production custom-domain routing and run the post-deploy site-consistency audit, including PDF conversion.
