2026-09-11T21:34+01:00
Promote canary-verified checker-start instrumentation correction to production.

Measured hypothesis:
- durable funnel shows 8 genuine landings, 0 recorded checker starts, 1 upload and 1 genuine analysis;
- inspection found the landing-page header "Get started" button changes React state but does not emit checker_cta_clicked, while the hero CTA already does;
- this creates an impossible-looking landing -> CTA -> upload sequence and understates genuine checker starts for users who enter through the header.

Change:
- instrument only the otherwise-untracked header "Get started" route;
- preserve canonical src attribution and same-origin /api/event delivery;
- do not add another tracker to the hero CTA, avoiding double counting;
- QA/demo/headless traffic remains excluded centrally;
- no personal data is added and no product/evidence claims change.

Canary verification:
- custom-domain canary #186 passed snapshot, Cloudflare deployment, domain/API verification and both mobile quote-intake paths;
- no AppDeploy deployment is involved;
- preserve current custom-domain routing and same-origin /api proxy.

Deploy via the existing guarded hqc-production workflow, then allow durable metrics to measure genuine traffic with the corrected entry-stage instrumentation.
