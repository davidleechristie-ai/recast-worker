2026-09-11T14:33+01:00
Promote acquisition-to-checker handoff optimisation.

Measured hypothesis:
- latest production snapshot: 346 landings -> 24 checker starts -> 11 uploads -> 3 genuine analyses;
- the earliest/largest measured constraint is landing -> checker start;
- several qualified organic cohorts are reaching the homepage from decision content but then recording zero checker starts.

This release removes a redundant second click for visitors who have already chosen a decision-page "check my quote" CTA. Same-origin decision-page links back to the HQC homepage now preserve their source attribution and add hqc_start=1, which opens quote intake automatically on arrival.

Guardrails:
- no new personal-data collection;
- no change to evidence, sizing, certification, MCS/BUS or grant-eligibility claims;
- no payment boundary or Stripe change;
- no DNS/routing change;
- QA traffic remains excluded from traction metrics.

Success measure: increased decision-content cohort checker starts and subsequent genuine analyses, not pageviews.

Acquisition handoff commit: ee84d20dee535cc1f2ea2900a60b308032397017
Canary run: 34604816866 (passed snapshot, custom-domain deploy, API/asset checks, QA exclusion and mobile upload/manual-entry browser paths).
