export default { async fetch(request) { const url = new URL(request.url); if (url.pathname === '/health') return new Response(JSON.stringify({ ok: true, service: 'hqc-migration-preview' }), { headers: { 'content-type': 'application/json' } }); return new Response('<!doctype html><html><body><h1>Home Quote Check Cloudflare migration preview</h1><p>GitHub to Cloudflare deployment path is working.</p></body></html>', { headers: { 'content-type': 'text/html; charset=utf-8' } }); } };
