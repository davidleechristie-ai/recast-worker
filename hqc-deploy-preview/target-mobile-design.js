/* Emergency rollback marker: Check my quote — free | aspect-ratio:1.94/1
   The compact mobile DOM-rewrite is intentionally disabled after the production blank-screen regression.
   Restore mobile compaction only after browser QA passes on preview. */
(()=>{})();
