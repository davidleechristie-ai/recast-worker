{
  "name": "recast-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker serving the Recast site plus real Stripe entitlement enforcement (KV-backed, webhook-verified).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node src/stripe-verify.test.mjs && node src/entitlements.test.mjs && node src/worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
